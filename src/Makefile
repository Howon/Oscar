TARFILES = Makefile scanner.mll parser.mly ast.mli oscar.ml


# More detailed: build using ocamlc/ocamlopt + ocamlfind to locate LLVM

OBJS = ast.cmx codegen.cmx parser.cmx scanner.cmx analyzer.cmx exception.cmx oscar.cmx

oscar : $(OBJS)
	ocamlfind ocamlopt -linkpkg -package llvm -package llvm.analysis $(OBJS) -o oscar

scanner.ml : scanner.mll
	ocamllex scanner.mll

parser.ml parser.mli : parser.mly
	ocamlyacc parser.mly

%.cmo : %.ml
	ocamlc -c $<

%.cmi : %.mli
	ocamlc -c $<

%.cmx : %.ml
	ocamlfind ocamlopt -c -package llvm $<

oscarulator.tar.gz : $(TARFILES)
	cd .. && tar zcf oscarulator/oscarulator.tar.gz $(TARFILES:%=oscarulator/%)

.PHONY : clean
clean :
	rm -f oscar prettyprint parser.ml parser.mli scanner.ml parser.output *.cmo *.cmi *.cmx *.o

# Generated by ocamldep *.ml *.mli
codegen.cmo : ast.cmo
codegen.cmx : ast.cmx
oscar.cmo : scanner.cmo parser.cmi codegen.cmo ast.cmo analyzer.cmo prettyprint.cmo exception.cmo
oscar.cmx : scanner.cmx parser.cmx codegen.cmx ast.cmx analyzer.cmx prettyprint.cmx exception.cmx
parser.cmo : ast.cmo parser.cmi
parser.cmx : ast.cmx parser.cmi
scanner.cmo : parser.cmi
scanner.cmx : parser.cmx
prettyprint.cmo : ast.cmo scanner.cmo parser.cmi
prettyprint.cmx : ast.cmx scanner.cmx parser.cmx
exception.cmo:
exception.cmx:
analyzer.cmo: ast.cmo sast.cmo exception.cmo prettyprint.cmo
analyzer.cmx: ast.cmx sast.cmx exception.cmx prettyprint.cmx
parser.cmi : ast.cmo
