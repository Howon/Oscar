def f1(num: int) => int = {
  return num * 2;
}

def f2(num: int) => int = {
  return num + 5;
}

def f3(num: int) => int = {
  return num - 20;
}

def main() => unit = {
    Println(f1(f2(f3(20))));
}
