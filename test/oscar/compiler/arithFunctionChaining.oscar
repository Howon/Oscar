def f1(num: int) => int = {
  return int * 2;
}

def f2(num: int) => int = {
  return int + 5;
}

def f3(num: int) => int = {
  return int - 20;
}

def main() => unit => {
  println(f1(f2(f3(20))));
}